namespace SoSmooth
{
    /// <summary>
    /// The ways that one contour can connect to another.
    /// </summary>
    public enum TargetPoint
    {
        Start,
        Finish,
        Nearest,
    }
}
